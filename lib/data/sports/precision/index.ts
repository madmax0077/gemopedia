import type { Sport } from "@/lib/types";

import { golf } from "./golf";

/** Precision sports category — archery, shooting, billiards, bowling, darts, golf. */
export const PRECISION_SPORTS: Sport[] = [
  golf,
];
